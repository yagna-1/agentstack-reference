import { test, expect } from '@playwright/test';

test('weather_agent_task_demo', async ({ page }) => {
  BLOCKED by AstraGraph: rule_id=unknown, score=1.00
  await page.keyboard.press("blocked");

});
